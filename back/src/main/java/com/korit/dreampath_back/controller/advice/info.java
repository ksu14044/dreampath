package com.korit.dreampath_back.controller.advice;

public class info {
    //comment.userId === loginUserData?.data?.userId
    // ?
    //      updateCommentValue.starPoint === -1
    //      ? comment.starPoint > idx ? 'on'
    //      : "" : updateCommentValue.starPoint > idx
    //      ? 'on' : ""
    // : comment.starPoint > idx ? 'on' : ""

    /*
    * if(comment.userId == loginUserData.data.userId){
    *   if(updateCommentValue.starPoint == -1) {
    *       if(comment.starPoint > idx) {
    *           sout("on");
    *       }else sout("");
    *   } else {
    *   if( updateCommentValue.starPoint > idx) {
    *       sout("on")
    *   } else sout("")
    * } else {
    *   if(comment.starPoint > idx) {
    *       sout("on");
    *   }else sout("");
    * }
    *
    * */

    /*
     * if(comment.userId == loginUserData.data.userId || updateCommentValue.starPoint != -1){
     * if( updateCommentValue.starPoint > idx) {
     *       sout("on")
     *   } else sout("")
     * } else {
     *   if(comment.starPoint > idx) {
     *       sout("on");
     *   }else sout("");
     * }
     * */
    //test
    // testtest
}
